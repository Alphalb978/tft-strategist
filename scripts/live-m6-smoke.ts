// M6 extends the bounded M5 smoke in-process so it proves the exact same
// acquisition/cache boundary followed by canonicalization, relation, lifecycle,
// persistence compatibility, and recommendation loading.
import './live-m5-smoke';
